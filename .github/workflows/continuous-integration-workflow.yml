name: Continuous Integration Workflow
on: [push]

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - run: docker build -t wled/test:latest . --target test
      - run: docker run wled/test:latest
      